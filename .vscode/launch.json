{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Debug with qemu using gdb",
            "executable": "",
            "target": ":1234",
            "remote": true,
            "autorun": [],
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText",
            "preLaunchTask": "preDebug setup"
        }
    ]
}